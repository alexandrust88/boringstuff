apiVersion: v2
name: envoy-gateway-wrapper
description: A Helm chart wrapper for Envoy Gateway with Azure LoadBalancer support
type: application
version: 1.0.0
appVersion: "1.2.0"

keywords:
  - envoy
  - gateway
  - api-gateway
  - ingress
  - azure

maintainers:
  - name: Platform Team
    email: platform@example.com

dependencies:
  - name: gateway-helm
    version: "1.2.x"
    repository: "oci://docker.io/envoyproxy"
    alias: envoy-gateway
